# GitHub 推送命令

## 在项目目录中运行以下命令：

```bash
# 添加远程仓库（请替换 YOUR_USERNAME 为您的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/skills-visualizer.git

# 验证远程仓库
git remote -v

# 推送代码到 GitHub
git push -u origin master
```

## 如果需要认证：
- 如果使用 HTTPS，可能会要求输入 GitHub 用户名和密码
- 建议使用 Personal Access Token（密码）